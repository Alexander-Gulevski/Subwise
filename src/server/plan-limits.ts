/**
 * Лимиты бесплатного тарифа — FR-11, ADR-0005.
 *
 * Значение продублировано в сид-скрипте фиче-флагов. Здесь оно живёт
 * как константа, потому что проверяется на горячем пути создания
 * подписки, а ходить в БД за числом на каждый вызов незачем.
 *
 * Порог подбирается на реальных данных (открытый вопрос Q5).
 */
export const FREE_PLAN_SUBSCRIPTION_LIMIT = 10;
